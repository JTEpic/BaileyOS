goals:
system clock
keyboard move cursor
0xE0 on keyboard
system error interrupts (eg divide by 0)
higher bit color mode
64bit long mode,new compiler/binutils/qemu,currently splits 64bit variables into 32bit components

handle_default_interrupt only EOI PIC2 if part of it
better drive number in boot loader
flat binary to only elf
readd ORG 0 to boot

(1)
bash make.sh

(2)
make
make qemu

(3)
bash debug.sh
make clean

xxd for binary data

https://wiki.osdev.org/FAT
Dragon Zap Education
https://github.com/screeck/YouTube/tree/main/How_to_write_a_bootloader
https://github.com/cfenollosa/os-tutorial

Tables/Interrupts
http://www.ctyme.com/intr/int.htm
https://intermezzos.github.io/book/second-edition/vga/understanding-text-mode.html
https://wiki.osdev.org/PS/2_Keyboard

Global Descriptor Table (GDT)
https://wiki.osdev.org/Global_Descriptor_Table
https://notisrac.github.io/FileToCArray/


Cross Compiler:
https://wiki.osdev.org/GCC_Cross-Compiler
    Put extracted tar.gz into $HOME/src:
    https://ftp.gnu.org/gnu/binutils/
    https://ftp.gnu.org/gnu/gcc/
https://github.com/bminor/binutils-gdb
https://github.com/gcc-mirror/gcc

Put Binutils and GCC source code into $HOME/src folder (github repo may not work), -j 8

export PREFIX="$HOME/opt/cross"
export TARGET=i686-elf
export PATH="$PREFIX/bin:$PATH"

For Binutils:

    cd $HOME/src

    mkdir build-binutils
    cd build-binutils
    ../binutils-x.y.z/configure --target=$TARGET --prefix="$PREFIX" --with-sysroot --disable-nls --disable-werror
    make -j 4
    make install

    cd ..

For GCC:

    cd $HOME/src

    # The $PREFIX/bin dir _must_ be in the PATH. We did that above.
    which -- $TARGET-as || echo $TARGET-as is not in the PATH

    mkdir build-gcc
    cd build-gcc
    ../gcc-x.y.z/configure --target=$TARGET --prefix="$PREFIX" --disable-nls --enable-languages=c,c++ --without-headers
    make -j 4 all-gcc
    make -j 4 all-target-libgcc
    make install-gcc
    make install-target-libgcc

Delete $HOME/src folder

Verify:

    $HOME/opt/cross/bin/$TARGET-gcc --version
    #i686-elf-gcc --version